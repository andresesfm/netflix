import React from 'react';
import { Footer, SigninForm, Header } from '../components';

const Signin = () => {
    return <><Header ><SigninForm title='Sign in' /></Header><Footer /></>;
};

export default Signin;
